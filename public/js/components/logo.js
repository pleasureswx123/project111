import '../../less/logo.less'
import React, { Component, PropTypes } from 'react';
import { render,findDOMNode } from 'react-dom'
class Logo extends Component {
    render() {
        return (
            <div className="miuiLogo">
            </div>
        );
    }
}

export default Logo